# Default values shown in the extension settings during local development.
# Do not put real API keys in this file.
VITE_DEFAULT_API_BASE_URL=https://ai.leihuo.netease.com/v1
VITE_DEFAULT_MODEL=gemini-3.1-pro-preview-customtools

# Optional local-only key for private development builds.
# Keep the real .env file out of git.
VITE_DEFAULT_API_KEY=
